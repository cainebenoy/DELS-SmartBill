import 'package:flutter/material.dart';

class AppColors {
  // Tailwind-like palette from design docs
  static const primary = Color(0xFF1A1C22);
  static const secondary = Color(0xFF5A5C6A);
  static const accent = Color(0xFFA7A8B2);
  static const backgroundLight = Color(0xFFF6F7F8);
  static const backgroundDark = Color(0xFF101922);
}
